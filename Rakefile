desc "Run app.rb with ruby"
task :app do
  sh "ruby app.rb"
end

desc "Run app.rb with shotgun"
task :shotgun do
  sh "shotgun app.rb"
end

desc "Run app_named_pars.rb with shotgun"
task :namedpar do
  sh "shotgun app_named_pars.rb"
end
